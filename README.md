This challenge is based on Heap Exploitation
First analyze the the given binary file using ghidra then we will see loading numbers and base conversion which gives us hint for exploiting heap 
Load , convert and delete numbers and then verify system integrity
Then choose show numbers to get the flag
